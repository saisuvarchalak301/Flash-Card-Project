<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.flashlearn">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlashLearn"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Flashcard View Activity -->
        <activity
            android:name=".FlashcardViewActivity"
            android:exported="true">
        </activity>

        <!-- Flashcard Creation Activity -->
        <activity
            android:name=".FlashcardCreationActivity"
            android:exported="true">
        </activity>

        <!-- Flashcard Edit Activity -->
        <activity android:name=".EditFlashcardActivity">
            <!-- Optional: You can add other configurations if needed -->
        </activity>


    </application>

</manifest>
